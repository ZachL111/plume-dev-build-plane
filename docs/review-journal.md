# Review Journal

The cases below are the review handles I would use before changing the implementation.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its developer tools focus without claiming live deployment or external usage.

## Cases

- `baseline`: `change width`, score 184, lane `ship`
- `stress`: `diagnostic quality`, score 187, lane `ship`
- `edge`: `review cost`, score 207, lane `ship`
- `recovery`: `safe rewrite`, score 172, lane `ship`
- `stale`: `change width`, score 200, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.
